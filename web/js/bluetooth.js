/*document.addEventListener('pointerup', function(event) {
    navigator.bluetooth.requestDevice({ filters: [{ name: "Smart Buoy #1" }] })
    .then(device => {
        // Human-readable name of the device.
        alert(device.name);

        // Attempts to connect to remote GATT Server.

    })
});*/